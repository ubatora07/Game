# CLEANUP MANIFEST — Workspace Sanitization

Date: 2026-08-20  
Target: Canonical Single Game Workspace (`ubatora07/Game`)

---

## 1. KEEP (Canonical Production Assets & Code)

### Root Configuration & Entry Points
- `index.html` — Web application entry point.
- `package.json` — Single canonical dependency tree and QA audit scripts.
- `package-lock.json` — Exact dependency lockfile.
- `tsconfig.json` — TypeScript compiler configuration.
- `vite.config.ts` — Vite build & bundler configuration.
- `playwright.config.ts` — Playwright end-to-end testing configuration.
- `.gitignore` — Source control ignore rules (updated for clean workspace).
- `DESIGN.md`, `PLAN.md`, `PLAN_RPG_EXPANSION.md`, `CHECKLIST.md`, `CHECKLIST_RPG_EXPANSION.md` — Root roadmap documents maintained under governance audit.

### Source Code (`src/`)
- `src/main.ts` — Runtime bootstrap and core systems wiring.
- `src/content/` — Catalogs (classes, skills, enemies, bosses, items, buildings, quests, relics, events, etc.).
- `src/core/` — Core game state, EventBus, combat, formulas, save contracts.
- `src/economy/` — Economic simulation engine.
- `src/services/` — Platform, Ads, Analytics, Audio, I18n, Save (V7).
- `src/systems/` — Gameplay systems (Campaign, Heroes, Mercenaries, Settlement, Pets, Tower, Rebirth, etc.).
- `src/ui/` — Components, screens, modals, navigation (IA V3), art registries, CSS tokens & layout.

### Tests & QA (`tests/`, `e2e/`, `scripts/`)
- `tests/` — Full Vitest test suite (99+ test suites covering all domain systems, contracts, and progression).
- `e2e/` — Playwright browser interaction and responsive tests.
- `scripts/` — Automated QA audit scripts (`governance-audit.cjs`, `source-production-safety-audit.cjs`, `terminology-audit.cjs`, `terminology-baseline.json`, `i18n-audit.cjs`, `content-coherence-audit.cjs`, `meta-content-audit.cjs`, `art-registry-audit.cjs`, `ui-production-audit.cjs`, `release-safety-audit.cjs`, `package-release.cjs`, `generate-balance-v4-csvs.cjs`, `generate-marketing-assets.cjs`).

### Documentation & Balance Artifacts (`docs/`)
- `docs/` — Architecture specifications, Art Bible, Governance, Runbooks, Balance CSVs and reports.

---

## 2. DELETE (Junk, Temporary Files, Duplicates, Reproducible Outputs)

### Archive Files in Workspace
- `anime-infinite-ascension.zip`
  - *Reason:* Stale zip archive in root directory; release archive is reproducibly generated by `npm run package:release`.

### Scratch / Temporary Scripts
- `qa-debug.cjs`
  - *Reason:* One-off Playwright manual debug script from initial development; superseded by `e2e/interaction-qa.spec.ts`.
- `check_plan.cjs`
  - *Reason:* One-off checklist regex batch script; no longer needed.

### Reproducible Build & Test Outputs
- `dist/`
  - *Reason:* Reproducible build output from `npm run build`; should be built on demand and gitignored.
- `test-results/`
  - *Reason:* Reproducible test artifact directory from Playwright/Vitest test runs; gitignored.

---

## 3. MERGE / MOVE
- *None needed:* All canonical domain, UI, and content files are already consolidated at the repository root.

---

## 4. UNKNOWN (Files Retained Due to Insufficient Deletion Proof)
- *None:* All files in workspace have clear, verifiable provenance and usage contracts.
